# Strata Environment Configuration Template
# Copy this file to .env for local overrides if needed.
# Note: In Phase 0, no environment variables are strictly required to run tests or start the server.

STRATA_ENV=development
STRATA_HOST=127.0.0.1
STRATA_PORT=8000
STRATA_DATA_DIR=data
